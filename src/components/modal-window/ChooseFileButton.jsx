import React from 'react';

import styles from './Modal-window.module.css';

const ChooseFileButton = () => {
    return (
        <button className={styles.btnChooseFile}>Choose file</button>
    );
};

export default ChooseFileButton;